/**
 * A valid password that meets all requirements:
 * - At least 8 characters
 * - Contains a number
 * - Contains a special character
 */
export const DEFAULT_VALID_PASSWORD = "TestPass123!";

/**
 * An invalid password that fails requirements (too short, no special char).
 */
export const DEFAULT_INVALID_PASSWORD = "weak";
